<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aimers.zone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="m">


        <activity android:name="com.aimers.zone.PayActivityTest" />
        <activity
            android:name="com.aimers.zone.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.aimers.zone.NotificationShowActivity" />
        <activity android:name="com.aimers.zone.MatchActivity" />
        <activity android:name="com.aimers.zone.NotificationActivity" />
        <activity android:name="com.aimers.zone.WalletActivity" />
        <activity android:name="com.aimers.zone.SignActivity" />
        <activity
            android:name="com.aimers.zone.MainActivity"
            android:theme="@style/CustomTheme" />
    </application>

</manifest>